{% extends 'base.html.twig' %}

{% block title %}Gestion des campus{% endblock %}

{% block body %}
<h1>Ajouter un campus</h1>

<div>
    {{ form_start(campusForm)}}
    {{ form_widget(campusForm)}}
    <button>enregistrer</button>
    {{ form_end(campusForm)}}

</div>


    <hr>
    <p>Tu veux voir les campus? </p>
    <form action="{{ path('campus_index') }}">
        <button type="submit">Clic ici</button>
    </form>

{% endblock %}